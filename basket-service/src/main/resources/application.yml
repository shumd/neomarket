spring:
  application:
    name: basket-service
  datasource:
    url: jdbc:postgresql://localhost:5432/basket_service
    username: basket_user
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true