app:
  customer-role-type-id: 2
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/neomarket
    username: postgres
  jpa:
    properties:
      hibernate:
        default_schema: users
  data:
    redis:
      port: 6379
      host: localhost